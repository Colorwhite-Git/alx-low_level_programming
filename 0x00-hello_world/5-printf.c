#include <stdio.h>
#include <string.h>
/**
 * main - print exactly
 * Description: The printf() function is used
 * Return: Always (0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
